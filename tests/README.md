# Tests

This directory contains tests for the Starter Onboarding Flow project.

## Structure

- `unit/`: Unit tests
- `integration/`: Integration tests

## Running Tests

To run the tests:

```bash
pytest
```

Or with coverage:

```bash
pytest --cov=app
```